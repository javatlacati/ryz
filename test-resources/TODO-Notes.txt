- To remove exception in the byte code use something like javassist see: so/4337281
- Support multiple public class in the same source
- Create attribute and don't duplicate it when defined in method :-S
- Create Bool class and simple blocks of code.
- Args, varargs, named args
- extension methods
- get/set properties like generation

- Indicate what lines haven't been processed 
- Compile using a classpath  ( to load existing classes ) 
- Work on blocks.
    - Block literal
    - Block invocation
    - Block identification

- Extension methods 
    - For core libraries ( if, for, while etc )
